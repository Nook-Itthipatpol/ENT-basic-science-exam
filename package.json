{
  "name": "ent-r1-mock-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "for f in js/*.js js/sets/*.js scripts/*.mjs; do [ -f \"$f\" ] || continue; node --check \"$f\" || exit 1; done",
    "add-set": "node scripts/add-set.mjs",
    "test": "node --test test/*.test.js",
    "start": "npx serve ."
  }
}
